# DISABLED: This workflow is disabled due to repository ownership restrictions
# To re-enable, transfer repository ownership to "hersouls" organization

name: Release (DISABLED)

# on:
#   push:
#     tags:
#       - 'v*' # v1.0.0, v1.1.0 등

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: SMS_V.2.0/package-lock.json
        
    - name: Install dependencies
      run: |
        cd SMS_V.2.0
        npm ci
        
    - name: Build application
      run: |
        cd SMS_V.2.0
        npm run build
        
    - name: Create Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        draft: false
        prerelease: false
        
    - name: Upload Release Assets
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: SMS_V.2.0/dist/
        asset_name: moonwave-sms-${{ github.ref_name }}.zip
        asset_content_type: application/zip